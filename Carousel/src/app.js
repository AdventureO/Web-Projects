import Carousel from "./carousel";

let images = [
    'images/fly.jpg',
    'images/bee.jpg',
    'images/bug.jpeg'
];

new Carousel(document.body, images);
new Carousel(document.body, images);
new Carousel(document.body, images);
new Carousel(document.body, images);
new Carousel(document.body, images);
new Carousel(document.body, images);